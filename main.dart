import 'account.dart';
import 'post.dart';

void main() {
  var newPost = Post();
  var account = Account();
  String? newName = null;
  int len = newName?.length ?? 0;
  if (newName != null) {
    int len = newName.length;
  }
  // if (account.name != null) {
  //   int len = account.name!.length;
  // }
}
